# ─────────────────────────────────────────────────────────────
# BUSCATOOLS — web-react
# Copiar a .env y completar.  .env NUNCA se commitea.
#
# Ambas variables son PÚBLICAS por diseño (viajan al navegador).
# La seguridad real la da Row Level Security en Supabase, NO el
# ocultamiento de estas claves.
#
# NUNCA agregar acá: service_role, OPENAI_API_KEY, ANTHROPIC_API_KEY
# ni ningún otro secreto. Eso va en Edge Functions.
# ─────────────────────────────────────────────────────────────

# Proyecto Supabase NUEVO — nunca el legacy
VITE_SUPABASE_URL=https://uaxcfufvapzulqvynanp.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_rw9cMuxu6rPtLGyubr5Qog_qrX0VzjY

# Solo build de producción. En CI lo inyecta el workflow con el nombre
# real del repo. En local dejar vacío (Vite usa '/').
# VITE_BASE_PATH=/web-react/
