# dependencias
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
*.local

# entorno — NUNCA commitear
.env
.env.local
.env.*.local

# editor / SO
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# test / cache
coverage
.vite
.eslintcache

# artefactos temporales de los scripts de seed
.tmp-*.json
muestra.json

# Estado del importador de catálogo (Fase 3.5)
import-catalog.state.json

# Clave administrativa de migración (service_role). NUNCA se commitea.
.env.migration

# Contraseñas de las cuentas de prueba de RLS. Nunca se commitea.
.env.rls*

# El snapshot de las series que dejan los fixtures de revisión entre corridas.
scripts/.fase6-fixtures-series.json
scripts/.fase7-entrega2-series.json
